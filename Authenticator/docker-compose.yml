version: '3.8'
services:
  authenticator:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./db:/usr/src/app/db
      - ./logs:/usr/src/app/logs
    restart: unless-stopped
